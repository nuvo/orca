# .travis.yml
language: go
go:
- 1.11.2

services:
- docker

after_success:
- if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then exit 0; fi
- test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash
